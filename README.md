 `git clone https://github.com/dorsho88/sb-component.git`
 
 `cd sb-component`
 
 `npm install`
 
 `npm start`

Runs the app in the development mode.<br>
Open [http://localhost:3000](http://localhost:3000) to view it in the browser.


